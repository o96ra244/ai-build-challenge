# AGENTS.md

## プロジェクト方針

- このリポジトリは、15作品を公開する単一のNext.jsサイトです。
- 各作品は `app/works/NN-slug/` に追加します。`NN` は2桁の作品番号、`slug` は内容を表す英小文字の識別子とします。
- 既存作品を壊さず、レビュー可能な小さな変更として作品を追加します。
- `main` へ直接コミットしません。作業ブランチからPRを作成します。
- PRを自動マージしません。

## 実装ルール

- TypeScriptの型安全性を維持します。
- 正当な理由がない限り `any` を使用しません。
- 不要な依存パッケージを追加しません。
- Server Componentを基本とし、必要な範囲だけClient Componentにします。
- セマンティックHTMLを使用します。
- キーボード操作と `:focus-visible` の視認性を確保します。
- 色だけに依存した状態表現を使いません。
- 動的領域の寸法を確保し、CLSを抑えます。
- 秘密情報、APIキー、個人情報をコミットしません。

## Environment safety

- リポジトリ外の環境を無断で変更しません。
- Homebrew、npmのグローバルインストールなど、システム全体へ影響するパッケージの追加・更新・削除を行いません。
- 既存のCLIがない場合は、接続済みツールやリポジトリ内の依存関係で代替できるか確認します。
- 代替できない場合は作業を止め、必要なツール、理由、影響範囲を利用者へ報告します。
- 利用者の明示承認なしに、既存のツールやパッケージを削除しません。
- リポジトリ外のファイルを変更しません。

## 必須検証

変更後は次のコマンドを実行します。

```bash
npm run lint
npm run typecheck
npm run test
npm run build
```

実施していない検証を成功と報告しません。実行できない場合は理由と代替確認手順を記載します。

## SEO / Social sharing

- 公開作品ごとに固有のtitle / descriptionを設定します。
- canonical URLを設定します。
- Open Graph metadataを設定します。
- X向け`summary_large_image`を設定します。
- OGP画像は原則として最終完成画面から作成します。
- OGP画像は1200×630を基本とします。
- browser chromeや個人情報を画像へ含めません。
- OGP画像のaltを設定します。
- 架空URLを使用しません。
- Preview URLをcanonicalにしません。
- 実HTMLでmetadata出力を検証します。
- OG画像URLのHTTP 200と寸法を確認します。
- ライセンス対象素材が画像へ含まれる場合、必要な帰属を維持します。
- X投稿前にOGPを確認します。
